# 作業ログ

## 2026-09-05 初回構築

Java業務系Webアプリ案件の応募準備として、Spring Boot の CRUD サンプルを新規作成。

### 使った外部リソースと規約確認

いずれも 2026-09-05 に一次情報を確認。すべて無償・商用利用可。

| リソース | ライセンス | 確認先 |
|---|---|---|
| Eclipse Temurin JDK 21 | GPLv2 + Classpath Exception | https://adoptium.net/docs/faq/ 「provided at no cost to you by Adoptium to use, forever」 |
| Spring Boot 4.1.1 | Apache License 2.0 | https://github.com/spring-projects/spring-boot/blob/main/LICENSE.txt |
| H2 Database | MPL 2.0 / EPL 1.0 デュアル | https://www.h2database.com/html/license.html 「You can use H2 for free ... including in commercial applications」 |
| start.spring.io | Spring公式の無料プロジェクト生成サービス | 雛形生成に1回だけ利用。連続リクエストなし |
| Maven Central | 依存ライブラリ取得 | 通常のビルド1回分のみ。並列取得なし |

Oracle配布のJDKは利用していない（有償/無償条件の変遷が読みにくいため）。
Oracle側の現在の条件は未確認。

### 実施内容

1. JDK未導入だったため `winget install EclipseAdoptium.Temurin.21.JDK` で導入
   → `openjdk version "21.0.12.1" 2026-08-18 LTS` を確認
2. start.spring.io から Maven プロジェクトを生成
   （web / thymeleaf / data-jpa / validation / h2 / devtools）
3. 社員名簿の CRUD ＋ 検索 ＋ ページング ＋ CSV出力を実装
4. 画面CSSは ui-designer エージェントに委譲
5. JUnit テスト 16件を作成、全件成功

### 詰まった点と原因（対症療法を避けた記録）

**(1) Maven の依存ダウンロードが PKIX エラーで失敗**

- 症状: `PKIX path building failed ... unable to find valid certification path`
  同時に winget の msstore ソースでも「サーバー証明書が予期された値に一致しない」
- 実測: `repo.maven.apache.org` に TLS 接続して証明書を確認したところ、
  発行者が `CN=Norton Web/Mail Shield Root, OU=generated by Norton Antivirus for SSL/TLS scanning`
  だった。Norton が HTTPS を復号して検査している
- 原因: そのルート証明書は Windows の証明書ストア（LocalMachine\Root, CurrentUser\Root、
  拇印 6591EA0578B8C0EF010EE1A806946CBB2D93F20F）に登録済みだが、
  **Java は自前の信頼リスト（cacerts）を使うため参照していない**
- 外部事例: 同種の事例を確認（企業プロキシ・ウイルス対策ソフトのSSL検査で同一症状）
  https://docs.mend.io/wsk/pkix-path-building-failed-sun-security-provider-ce
  https://www.qcecuring.com/blog/fix-pkix-path-building-failed-java
  https://putridparrot.com/blog/adding-certificates-to-the-java-cacerts-or-fixing-pkix-path-issue/
- 対策: `.mvn/jvm.config` に `-Djavax.net.ssl.trustStoreType=WINDOWS-ROOT` を追加。
  JDK本体の cacerts は書き換えていない（JDK更新で消えるうえ、影響範囲が広いため）
- 採用しなかった案: `-Dmaven.wagon.http.ssl.insecure=true`（証明書検証を切るだけの対症療法）

**(2) `spring-boot-starter-parent:4.1.1.RELEASE` が見つからない**

- 原因: start.spring.io の metadata が返すバージョンIDは `4.1.1.RELEASE` 形式だが、
  Maven Central の実際の成果物は `4.1.1`。生成時に指定値がそのまま pom に入っていた
- 確認: https://repo.maven.apache.org/maven2/org/springframework/boot/spring-boot-starter-parent/maven-metadata.xml
  に `4.1.1` は存在、`4.1.1.RELEASE` は存在しない
- 対策: pom.xml の parent version を `4.1.1` に修正

**(3) テストのアノテーションが解決できない**

- 症状: `org.springframework.boot.test.autoconfigure.orm.jpa` が存在しない
- 原因: Spring Boot 4 でテストスライスがモジュール分割され、パッケージが移動していた。
  記憶で書いたパッケージ名が Boot 3 系のものだった
- 確認: ローカルリポジトリの jar を実際に開いてクラス名を確認
  - `org.springframework.boot.data.jpa.test.autoconfigure.DataJpaTest`
  - `org.springframework.boot.webmvc.test.autoconfigure.WebMvcTest`
- 対策: import を実物に合わせて修正

### 検証結果

起動して実際にHTTPを叩いて確認（ログではなく実レスポンスを確認）。

| 確認項目 | 結果 |
|---|---|
| 一覧表示（12件） | OK |
| キーワード検索（氏名・フリガナ・社員番号） | OK |
| 部署で絞り込み（開発部→4件） | OK |
| 在籍区分で絞り込み（退職→1件） | OK |
| ページング（2ページ目にE0011/E0012） | OK |
| 入力チェック5種が同時に出る | OK |
| 社員番号の重複検出 | OK |
| 新規登録→302リダイレクト→一覧反映 | OK |
| 編集画面に既存値が入る | OK |
| 更新の反映 | OK |
| 退職処理（行は残り区分だけ変わる） | OK |
| 物理削除 | OK |
| CSV出力（BOM付き / CRLF / 全項目クォート / 14行） | OK |
| 検索条件付きCSV（画面の絞り込みと件数一致） | OK |
| JUnit | 16件 全成功 |

### 未実施

- ログイン機能なし（誰が更新したかを記録していない）
- Flyway 等のDBマイグレーション管理なし（`ddl-auto=update` のまま）
- 排他制御なし（同じ社員を2人が同時に編集すると後勝ち）
- 本番向け設定の分離なし（H2コンソールが有効なまま）

いずれも「なぜ入れていないか」を説明できる状態にはしてある。
面談で「次に何を足すか」を聞かれたら、この4点から答える。

### 追加実施（同日）

- 存在しないIDのURLを直接開くと500になっていたため、`GlobalExceptionHandler` で
  404 + 日本語メッセージ画面（`templates/error/message.html`）に差し替え。
  実測: `/employees/9999/edit` → HTTP 404 /「データが見つかりません」を確認
- 画面CSS（`static/css/app.css`）は ui-designer エージェントに委譲。
  グレースケール地に青1色（#14538c）のみ。影・グラデーション不使用。
  コントラスト比は全てWCAG AA超え。列名はスクロール時に固定
- Chromium で実際に3画面を描画して目視確認済み（一覧 / 新規登録 / 入力エラー表示）。
  スクリーンショットは `デスクトップ整理\AI出力\社員名簿アプリ_画面\` に保存
- コンソールエラーは favicon の404のみ（機能に影響なし）

## 2026-09-05 追記：Norton対策を恒久化

### 調べたこと（確認日 2026-09-05）

「Norton側で検査を止められないか」を確認した。結論は**止める設定は用意されていない**。

- 実測: `curl` で同じサイトに接続しても発行者は `Norton Web/Mail Shield Root`。
  傍受はブラウザ限定ではなく全プロセスに一律。
  curl が成功していたのは Schannel（Windowsの証明書ストア）を使うため
  （`curl 8.19.0 ... Schannel` を確認）
- 公式「ノートン セーフウェブのオンとオフを切り替える」
  https://support.norton.com/sp/en/us/home/current/solutions/v45748162
  → **ブラウザ拡張機能の話しか書かれていない**。ネットワーク層のHTTPS検査を止める手順は無し
- Norton公式コミュニティ 2025-10-04
  https://community.norton.com/t/where-are-ssl-tls-scanning-settings-in-norton-360/447500
  → 「Nortonが自前の証明書を使うのを止める方法は提供されていない」
- Norton公式コミュニティ 2026-09-01（本件と同一の証明書についての報告）
  https://community.norton.com/t/safe-web-exclusions-dont-prevent-tls-interception/528263
  → セーフウェブの除外設定に入れてもTLS検査は止まらない。止めるにはセーフウェブごと無効化

セーフウェブ無効化は不正サイトのブロック機能を落とすため採用しない。
**このPCでセーフウェブを切った場合に実際に止まるかは未検証**（設定を変更していないため）。

### 実施したこと

Nortonのルート証明書を JDK の信頼リスト（cacerts）に取り込む方式に変更。
Windowsが既に信頼している証明書をJava側にもそろえるだけで、信頼範囲は広がらない。

- `_norton-cert\norton-root.cer` に証明書を書き出し（拇印 6591EA05...F20F）
- `import-norton-cert.ps1` を管理者権限で実行し、
  alias `norton-web-mail-shield-root` として登録
  （SHA-256 4E:13:FA:50:...:EC:E5:7D）
- cacerts は `cacerts.backup-before-norton` に退避済み
- 元に戻す `uninstall-norton-cert.ps1` を用意
- プロジェクトの `.mvn/jvm.config` は**削除**。
  コードは環境依存の設定を持たない状態に戻った

### 検証（応急処置を外した状態で実施）

- 素のJavaで `https://repo.maven.apache.org/` に接続 → `trustStoreType = 未指定（JDK既定）` で HTTP 200
- `mvnw dependency:get` で未キャッシュのライブラリを新規ダウンロード → 成功、PKIXエラーなし
- `mvnw test` → 16件全成功

### 副産物：アプリ起動中にテストが落ちる問題を修正

`mvnw test` を実行した際、`@SpringBootTest` が Hibernate の dialect 解決で失敗した。
原因は、起動中のアプリがH2のDBファイルを掴んでいたため。
`src/test/resources/application.properties` を追加し、
テスト時のみメモリDB（`jdbc:h2:mem:testdb` / `ddl-auto=create-drop`）を使うようにした。
アプリを起動したまま `mvnw test` を流して16件成功することを確認。

### JDKを更新したら

cacertsは初期化されるため `import-norton-cert.ps1` を再実行する。

---

## 2026-09-05 GitHubへの公開と、応募文の修正

### やったこと

**1. employee-app を GitHub に公開**

- リポジトリ: https://github.com/zhencishankou335-tech/employee-app
- 公開設定: public
- ブランチ: main（コミット3件をそのまま push）

公開の理由は、応募文に貼るURLが必要だったため。
それまでの応募文は「ソースコードはご希望があればお送りします」で終わっており、
相手に一手間かけさせる形になっていた。

**公開前に確認したこと**

- `application-prod.properties` に接続情報・パスワードの直書きが無いこと
  （すべて `${DB_URL}` 形式の環境変数参照。既定値も置いていない）
- `.gitignore` にDB実体（`*.mv.db` / `/data/`）とログが入っていること
- 追跡ファイル55件を目視。鍵・トークンの混入なし
- dev用ログイン情報（admin / viewer）はREADMEに公開前提で書いてあるもの。
  devプロファイルのときだけ作られ、prodでは作られないことをテストで固定済み

**公開後の実測**

- `gh repo view` → `"visibility":"PUBLIC"`
- 未ログイン状態の `curl` でトップページ HTTP 200
- 同じく raw の README.md も HTTP 200
  → 応募先の担当者がログインせずに開けることを確認した

**2. 応募文から「2本目」の記述を削除**

`資料/応募文_下書き.md` を修正。

- 「業務系のWebアプリを2本作ってみました」→「作ってみました」
- 「1本目は社員名簿…2本目は同じ手順で作った書籍管理アプリです」→ 社員名簿の説明のみ
- 本文にGitHubのURLを追加
- 「排他制御」→「同時編集の競合検知」に言い換え（採用担当が非エンジニアの場合を想定）

book-app を書かない判断の理由は、book-app が CRUD・検索・CSV・集計止まりで、
ログイン・Flyway・排他制御・prod設定の分離が未適用のため。
2本並べると、employee-app に入れた工夫が薄まる。
聞かれたときに口頭で「もう1本あります」と答えれば足りる。

### 残っていること

- 応募文の【氏名】と稼働時間を本人が記入して、クラウドワークスから送信
- READMEにスクリーンショットが入っていない。
  `資料/` に画面キャプチャ9枚（01〜04, 07〜11）があるが、リポジトリには含めていない。
  相手がアプリを起動せずに中身を判断できるようにするなら、入れる価値がある（未実施）

## 2026-09-05（追記）募集要項の実物が出てきたため応募文を作り直し

ユーザーが実際の募集文を提示。**指定の応募フォーマットがあった**ため、
自由記述で書いていた応募文はそのままでは使えないことが判明した。

指定フォーマット（募集文からの引用）:
氏名 / 年齢 / 居住エリア（都道府県）/ Javaの学習歴 / 触れたことのある技術 /
ポートフォリオ（GitHubなどのURL）/ 稼働開始可能時期 / 意気込みや自己PR

### 前の下書きからの変更点

1. **指定の8項目に組み替えた。** 項目の増減はしない
2. **「ポートフォリオ」欄に GitHub の URL を入れた。**
   募集文自身がURLを求めているので、外部誘導とは見なされない
3. **「触れたことのある技術」を追加。**
   募集文の例（Java／Spring Boot／SQL／Git）と同じ粒度に合わせ、
   employee-app に実際に入っているものだけを書いた
4. **契約形態・常駐/在宅の質問を応募文から削除した。**
   募集文に「オンライン面談（15〜30分）で、学習状況やご希望を伺います」とあるため。
   指定フォーマットを崩して応募段階で条件を聞くと、募集文を読んでいない人に見える。
   質問は面談用のメモに移した
5. **「長期で稼働できます」は書かなかった。**
   募集文に「応募内容に虚偽があった場合、契約を見送る」条項があり、
   本人が確約できるか分からないため。稼働開始可能時期の記入で足りる

### 本人が埋める箇所（4→5か所に増えた）

氏名 / 年齢 / 居住エリア / Javaの学習歴 / 稼働開始可能時期

**学習歴は盛らないこと。** 募集条件は「方法や期間は問いません」なので期間の短さは不利にならない。
虚偽条項があるほうがリスクが大きい。

### ファイル

- 新: `資料/応募文_下書き.md`（指定フォーマット版）
- 旧: `資料/旧_応募文_自由記述版_未使用.md`（参照用に残置）
- AI出力: `デスクトップ整理/AI出力/応募文_Java業務系案件_クラウドワークス_指定フォーマット版.md`
  ※ 旧版のコピー `応募文_Java業務系案件_クラウドワークス.md` は削除しようとしたが
    別プロセスが掴んでいて消せなかった（Device or resource busy）。手で消すこと

### 未実施のまま残っている件

READMEにスクリーンショットが無い。
**募集文が「ポートフォリオ：GitHubなどのURL」を明示的に求めている以上、
採用担当はこのURLを開いて判断する。** アプリを起動してもらえる前提は置けないので、
READMEの冒頭に画面を数枚並べる価値は前より上がっている。
`資料/` に employee-app の画面キャプチャ9枚（01〜04, 07〜11）あり。

### 2026-09-05（さらに追記）「触れたことのある技術」を短くした

当初は `Java 21／Spring Boot（Spring Security・Spring Data JPA・Thymeleaf）／SQL／JUnit／Maven／Git・GitHub`
と書いていたが、ユーザーの判断で募集文の例と同じ粒度に短縮した。

確定版:
`・触れたことのある技術：Java 21／Spring Boot／SQL／JUnit／Maven／Git`

**pom.xml を実際に開いて確認したうえでの判断**（記憶ではなく現物）:
starter-security / data-jpa / thymeleaf / webmvc / validation / flyway、h2、`<java.version>21</java.version>`
がすべて入っており、当初の記載に虚偽は無かった。短縮は正確性ではなく見え方の問題。
募集文の例が「Java／Spring Boot／SQL／Git」の4つなので、
未経験の応募で技術名を長く並べると中身より先に「盛っている」印象が立つ、という理由。

なお当初の行には **Flyway が抜けていた**（自己PRに「テーブル定義の変更管理」と書いているのに
それを担う名前が無い状態だった）。短縮版では列挙自体をやめたので、この不整合も解消している。
面談で聞かれたときのために、実際に入っているものを `資料/応募文_下書き.md` の注意書きに列挙してある。

### この日の作業中に起こした事故と、その後始末（再発防止のため記録）

親リポジトリ `dejiina_agent` に、意図しないコミット `29716fa` を作ってしまった。
1905ファイル・221,560行削除を巻き込む内容だった。

**原因**: 1つのコマンドの中で
`cp A B && echo ... && cd employee-app && cat >> WORKLOG.md <<EOF ... EOF` と書き、
その **EOF の次の行**に `git add -A && git commit && git push` を置いた。
`cp` が失敗して `&&` の連鎖は途中で止まったが、
**改行で区切った `git add -A` は連鎖の外なので、そのまま実行された。**
しかも `cd` が実行されていないためカレントディレクトリは `資料/`＝親リポジトリで、
`git add -A` が親リポジトリ全体を巻き込んだ。

**後始末**: push は失敗していた（親のブランチは master なのに `git push origin main` と書いたため
`src refspec main does not match any` で停止）。リモートには何も出ていないことを
`git log origin/master..HEAD` で確認したうえで `git reset --mixed HEAD~1` で取り消した。
`--mixed` なので作業ツリーのファイルは変更していない。
HEAD が `4421502` に戻り、ステージ0件、未コミット1856件（元の状態）に戻ったことを確認済み。

**再発防止**: `git add -A` は使う場所を必ず `cd` と同じ `&&` 連鎖の中に入れる。
複数リポジトリが入れ子になっているツリーでは、コミット前に `git rev-parse --show-toplevel` で
どのリポジトリにいるかを確認する。

### 2026-09-06 学習歴が判明したので記入

ユーザーから「4月から開発スクールに入って学んでいる」と申告があった。
今日が2026-09-06なので約5ヶ月、かつ**まだ在学中**。

記入した内容:
`・Javaの学習歴：開発スクールにて2026年4月から学習中（約5ヶ月・在学中）`

**「卒業」ではなく「学習中」と書いた。** 実際に在学中のため。
募集文に「応募内容に虚偽があった場合、契約を見送らせていただくことがあります」とあり、
学習歴は面談で必ず掘られる項目なので、ここを盛るのは割に合わない。
募集条件は「独学・スクール・学校など、方法や期間は問いません」なので、
期間の短さも在学中であることも不利にならない。

自己PRの書き出しも変えた。
旧: 「Javaの実務経験はありません。学習として、生成AIツールを使いながら…」
新: 「Javaの実務経験はありません。今年4月から開発スクールで学んでおり、その学習の一環として…」
「実務経験なし」という否定で始まるより、「学んでいる最中で、その成果がこれ」と続くほうが読み手に通る。

**未確認のまま残っている前提**: 「4月」が2026年か2025年か。
2026年と解釈して記入した。2025年なら約1年5ヶ月になる。
資料/応募文_下書き.md の注意書きに、ここだけ本人確認するよう明記した。

**在学中であることの副作用**: 先方は「スクールと並行してどれだけ動けるか」を必ず気にする。
修了予定が決まっているなら稼働開始可能時期の欄に書くと先回りできる旨を注意書きに追記した。

本人が埋める箇所は 5→4か所に減った（氏名 / 年齢 / 都道府県 / 稼働開始可能時期）。

## 2026-09-06 本人がアプリを実機で確認（応募前の突き合わせ）

応募文に書いた内容が実物と合っているかを、ユーザー本人がブラウザで確認した。

### 結果：すべて一致

| 応募文の記述 | 確認方法 | 結果 |
|---|---|---|
| 一覧・検索・CSV出力 | 部署=開発部で検索 | 4件。2ページ目のE0011も拾った |
| 集計 | 部署別人数 | 営業3/開発4/経理2/人事2/総務1=12。在籍10/休職1/退職1 |
| ログイン | admin でログイン | 成功 |
| 権限による表示と操作の制限 | viewer でログイン | 新規登録ボタンと操作列が消滅 |
| 同上（サーバ側で止まるか） | viewer で /employees/new を直打ち | 403「この操作を行う権限がありません」 |
| テーブル定義の変更管理 | 起動ログ | Flyway「Current version of schema: 901」 |
| 同時編集の競合検知 | 2タブで同じ社員を編集し順に保存 | 後の保存が弾かれ、先の保存内容が残った |
| テストはJUnitで39件・全通 | mvnw test | Tests run: 39, Failures: 0, Errors: 0 |

### 途中で起きた問題と原因

**1. ブラウザが開かなかった**
`cmd.exe /c start http://localhost:8080` および `Start-Process` 経由の既定ブラウザ起動が
この環境では効かず、ユーザー側には何も表示されなかった。
`tasklist` で確認したところ chrome.exe / msedge.exe のプロセスが1つも無く、
msedgewebview2.exe（他アプリ埋め込み）だけだった。
**対処**: Chrome の実行ファイルを絶対パスで直接指定して起動 →
`Start-Process "C:\Program Files\Google\Chrome\Application\chrome.exe" -ArgumentList "http://localhost:8080"`
MainWindowTitle が「ログイン | 社員名簿」になることで起動を確認。

**2. ログインが弾かれた**
原因を推測で「打ち間違い」とせず、実測で切り分けた。
- 起動ログ: プロファイル=dev、Flyway=901適用済み → 設定は正常
- curl で CSRF トークンを取得して POST /login → **HTTP 302 → /employees**（＝認証成功）
これでアプリ側は正常と確定。入力側の問題（日本語入力オンによる全角化が最有力）と切り分けた。

**3. 手順が伝わらなかった**
「タブを右クリック→複製」と指示したが、ユーザーはアドレスバーを右クリックした。
タブストリップの位置を前提にした指示が悪い。
**対処**: `Ctrl+T` → URL直打ち → `Ctrl+Shift+Tab` に置き換えたら通った。
画面上の位置を指す指示より、キー操作のほうが誤解が少ない。

### 副産物：ワンクリック起動ファイルを作成

`社員名簿アプリを起動.bat`（employee-app 直下、AI出力にもコピー）
ダブルクリックで mvnw spring-boot:run を実行し、8080が応答したら Chrome を自動で開く。
黒い画面を閉じればアプリも止まる。
ユーザーが自力で起動できないと面談前の再確認ができないため用意した。

### 残っている汚れ

E0001 山田太郎の備考が確認作業で "A" になっている（元は空欄相当）。
dev用の確認データなので実害は無いが、面談前に見せるなら
`data/` フォルダを消して再起動すれば V900/V901 が入り直して初期状態に戻る。

## 2026-09-06 応募完了

クラウドワークスのメッセージで先方（Kan.H様）へ送信。投稿日時 2026-09-06 06:50。

**この案件は自分から応募したのではなく、先方から声をかけられたもの**（本文「今回お声がけ頂き」）。
応募フォームではなくメッセージ画面での返信だった。

送信直前に直したもの:
- 誤字「ありごあとございます」→「ありがとうございます」
- 記入欄の目印として付けていた【】を4か所すべて除去
  （【山口真次】のように括弧付きのまま送られかけていた。目印の書き方が悪かった）

### この日にやったこと（応募まで）

1. 学習歴の記入（開発スクール 2026年4月〜、在学中）。「卒業」と書かない
2. 本人によるアプリの実機確認。応募文の記述がすべて実物と一致することを確認
3. READMEに画面キャプチャ7枚を追加（未ログインでの取得を curl で確認済み）
4. 確認作業で汚れた dev データを初期化（data/ を削除して再起動）

### 次

先方からの返信待ち。返信が来たらオンライン面談（15〜30分）。
面談で聞くことは `資料/応募文_下書き.md` の「面談で必ず聞くこと」に5項目まとめてある。
契約形態（正社員か業務委託か）、常駐か在宅か、テスト工程の想定期間、
単価見直しの基準、現場で生成AIツールが使えるか。

**在学中である点は必ず突かれる**。スクールと並行してどれだけ動けるかを、
自分の中で先に決めておくこと。
